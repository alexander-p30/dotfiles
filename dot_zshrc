# vim:syntax=bash
zstyle '*:compinit' arguments -u

# znap {{{
[[ -r ~/.config/znap/znap-core/znap.zsh ]] ||
    git clone --depth 1 -- \
        https://github.com/marlonrichert/zsh-snap.git ~/.config/znap/znap-core

source ~/.config/znap/znap-core/znap.zsh

# Start prompt
znap eval starship 'starship init zsh --print-full-init'
znap prompt

znap eval mise '~/.local/bin/mise activate zsh'

# Plugins
znap source zsh-users/zsh-autosuggestions
znap source zsh-users/zsh-syntax-highlighting
znap source ohmyzsh/ohmyzsh \
  plugins/git \
  plugins/direnv

# From Prezto
znap source sorin-ionescu/prezto
znap source sorin-ionescu/prezto \
  modules/helper \
  modules/completion \
  modules/environment \
  modules/terminal \
  modules/editor \
  modules/history \
  modules/directory \
  modules/utility

# FZF
[ -f ~/.fzf.zsh ] && source ~/.fzf.zsh

# Completions
znap fpath _mise 'mise completion zsh'
znap fpath _remotectl 'remotectl completion zsh'
znap fpath _rustup 'rustup completions zsh'
znap fpath _cargo 'rustup completions zsh cargo'
znap fpath _colima 'colima completion zsh'
# }}}

# Aliases {{{
source ~/.alias/git-aliases
source ~/.alias/rails-aliases
source ~/.alias/soft-aliases
source ~/.alias/system-aliases
alias mise='nocorrect mise'
# }}}

# Keep IEx history
export ERL_AFLAGS="+MIscs 2048 -kernel shell_history enabled"

# Launch/attach the persistent herdr session for interactive shells
# (skip when already inside a herdr pane). Replaces the old tmux auto-attach;
# herdr passes TERM_PROGRAM through, so the OUTER_TERM_PROGRAM dance is gone.
if command -v herdr &> /dev/null && [ -n "$PS1" ] && [ -z "$HERDR_SESSION" ] && [ -z "$HERDR_ENV" ]; then
  herdr --session default
fi

# Setup zoxide
znap eval zoxide 'zoxide init zsh'


source ~/.scripts/gt.zsh
bindkey -s '^f' 'gt\n'
bindkey -s '^n' 'gt -n\n'

how() {
  claude --model haiku -p "Answer concisely and briefly: how $*"
}

export PATH="/opt/homebrew/opt/icu4c@77/bin:$PATH"
export PATH="/opt/homebrew/opt/icu4c@77/sbin:$PATH"

export PGDATA="/Users/estevanalexander/.local/share/mise/installs/postgres/17.7/data"

[ -f "$HOME/.atuin/bin/env" ] && . "$HOME/.atuin/bin/env"
command -v atuin &> /dev/null && eval "$(atuin init zsh --disable-up-arrow)"

# run arbitrary commands ```RUN='echo "it works"' zsh```
TEMP_RUN=$RUN
export RUN=
eval "$TEMP_RUN"
source ~/.safe-chain/scripts/init-posix.sh # Safe-chain Zsh initialization script
